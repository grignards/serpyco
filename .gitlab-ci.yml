tests-3.6:
  image: python:3.6
  script:
    - apt-get update -q -y
    - pip install -r requirements.txt
    - python setup.py install
    - pytest test.py

tests-3.7:
  image: python:3.7
  script:
    - apt-get update -q -y
    - pip install -r requirements.txt
    - python setup.py install
    - pytest test.py